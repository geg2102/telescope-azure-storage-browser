local finders = require('telescope.finders')
local az_finders = {}

-- Remove or comment out the blob_finder function
-- We don't need this anymore as we'll handle fetching blobs in the picker

return az_finders
